# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: complex-input-expressions-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: set image metadata
        id: meta
        shell: bash
        run: echo "version=latest" >> "$GITHUB_OUTPUT"
      - name: determine images
        shell: bash
        run: |
          IMAGES="${{ 'false' == 'true' && format('{0}/{1}', 'localhost:5000', 'myapp') || '' }}"
          echo "images=$IMAGES"
      - name: build and push
        shell: bash
        run: |
          PUSH="${{ github.ref == format('refs/heads/{0}', 'main') || 'false' == 'true' }}"
          echo "push=$PUSH"
