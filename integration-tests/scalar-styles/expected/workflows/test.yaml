# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: scalar-styles-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: literal block
        shell: bash
        run: |
          echo "Step 1: preparing production"
          echo "Step 2: building"
          echo "Step 3: done"
      - name: folded block
        shell: bash
        run: >
          echo "This is a long command for production that spans multiple lines"
      - name: plain string
        shell: bash
        run: echo "simple production"
