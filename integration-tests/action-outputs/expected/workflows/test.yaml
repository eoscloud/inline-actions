# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: action-outputs-test
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: build artifact
        id: build--set-output
        shell: bash
        run: |
          echo "Building version 2.0.0"
          echo "url=https://artifacts.example.com/2.0.0/app.tar.gz" >> "$GITHUB_OUTPUT"
          echo "checksum=abc123" >> "$GITHUB_OUTPUT"
      - name: download artifact
        shell: bash
        run: echo "Downloading from ${{ steps.build--set-output.outputs.url }}"
      - name: verify checksum
        shell: bash
        run: echo "Verifying checksum ${{ steps.build--set-output.outputs.checksum }}"
