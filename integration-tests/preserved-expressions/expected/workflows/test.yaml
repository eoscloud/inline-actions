# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: preserved-expressions-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: build with context
        shell: bash
        run: echo "Building testapp on ${{ github.ref }}"
      - name: use secrets
        shell: bash
        run: echo "Token is ${{ secrets.DEPLOY_TOKEN }}"
      - name: reference other steps
        shell: bash
        run: echo "Previous result was ${{ steps.build.outputs.result }}"
