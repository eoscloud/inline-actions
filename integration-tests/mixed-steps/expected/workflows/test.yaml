# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: mixed-steps-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: prepare
        run: echo "Preparing workspace"
      - name: compile
        shell: bash
        run: echo "Compiling in release mode"
      - name: package
        shell: bash
        run: echo "Packaging release build"
      - name: upload artifacts
        run: echo "Uploading artifacts"
