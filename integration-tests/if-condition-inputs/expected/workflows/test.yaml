# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: if-condition-inputs-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: run scan
        shell: bash
        run: echo "scanning ${{ steps.build.outputs.ref }}"
      - name: cleanup
        if: always() && steps.build.outputs.ref != ''
        shell: bash
        run: echo "cleaning up ${{ steps.build.outputs.ref }}"
      - name: notify
        if: "'true' == 'true'"
        shell: bash
        run: echo "notifying"
