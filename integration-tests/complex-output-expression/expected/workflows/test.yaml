# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: complex-output-expression-test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: compute path
        id: gen--meta
        shell: bash
        run: echo "path=items/latest" >> "$GITHUB_OUTPUT"
      - name: use output
        shell: bash
        run: echo "URL is ${{ steps.gen--meta.outputs.path }}"
