# NOTE: This file is auto-generated by inline-actions. Do not edit manually.
# Source: workflow-sources/test.yaml
name: multiple-actions-test
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: configure environment
        shell: bash
        run: echo "Configuring environment staging"
      - name: install dependencies
        shell: bash
        run: echo "Installing dependencies for staging"
      - name: build artifacts
        shell: bash
        run: echo "Building version 1.2.3"
      - name: push to target
        shell: bash
        run: echo "Deploying 1.2.3 to staging.example.com"
